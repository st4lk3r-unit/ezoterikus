{
  "name": "ezoterikus-client",
  "version": "0.1.0",
  "description": "Zero-trust deniable messaging client using Signal Double Ratchet",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "bundle:signal": "esbuild node_modules/@signalapp/libsignal-client/dist/browser.js --bundle --minify --format=esm --outfile=public/js/vendor/libsignal.esm.js"
  },
  "dependencies": {
    "@privacyresearch/libsignal-protocol-typescript": "^0.0.16",
    "@noble/curves": "^1.4.0",
    "@noble/hashes": "^1.3.2",
    "argon2-browser": "^1.18.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "esbuild": "^0.21.0"
  }
}
